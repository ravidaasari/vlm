<%= form_with(model: provider, local: true) do |form| %>
  <% if provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provider.errors.count, "error") %> prohibited this provider from being saved:</h2>

      <ul>
      <% provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :provider_name %>
    <%= form.text_field :provider_name %>
  </div>

  <div class="field">
    <%= form.label :provider_type %>
    <%= form.text_field :provider_type %>
  </div>

  <div class="field">
    <%= form.label :provider_url %>
    <%= form.text_field :provider_url %>
  </div>

  <div class="field">
    <%= form.label :provider_user %>
    <%= form.text_field :provider_user %>
  </div>

  <div class="field">
    <%= form.label :provider_password %>
    <%= form.text_field :provider_password %>
  </div>

  <div class="field">
    <%= form.label :provider_session %>
    <%= form.text_field :provider_session %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
